# Latest Agent Handoff

## Active Task

- **Task:** TASK-009 — Implement approved curriculum access
- **Task file:** `tasks/TASK-009.md`
- **Owner / Agent:** Antigravity

## Current Status

`DONE` — Structured activity schema, reversible database migration, import validation & idempotency, formal approval flow & immutable version forking, runtime access service with FR-006 draft/revoked rejection, PAUD pilot pack fixture, and complete unit test suite verified.

## What Was Completed

1. **Curriculum Activity Schema & Domain Contracts (`app/curriculum/models.py`):**
   - Implemented `ReviewStatus` (`DRAFT`, `APPROVED`, `REVOKED`, `ARCHIVED`) and `ActivityDifficulty` (`BEGINNER`, `INTERMEDIATE`, `ADVANCED`).
   - Implemented `AnswerSpec` with exact matches, synonyms, phonetic variations, case-sensitivity controls, non-empty validation, and token expansion.
   - Implemented `ActivityAudioReferences` linking prompt, hint, and pre-recorded offline fallback audio asset IDs (`ADR-009`).
   - Implemented `CurriculumActivity` defining module, skill, difficulty, prompt text, answer spec, scaffolding hints, gentle retry prompt, and review notes (`FR-007`).
   - Implemented `CurriculumManifest` package contract and `ActivityProvenance` audit DTO (`DATA-007`).

2. **Reversible Database Schema Migration (`alembic/versions/003_curriculum_activity_spec.py`):**
   - Added `activity_spec` JSON column to `curriculum_items` with server default `{}`.
   - Updated `CurriculumItem` model in `app/persistence/models.py`.
   - Verified reversible upgrade/downgrade cycle in `tests/migration/test_migrations.py`.

3. **Field-Level Importer & Validation (`app/curriculum/importer.py`):**
   - Implemented `CurriculumImporter` providing pre-import schema validation and returning detailed field-level errors.
   - Enforced idempotency for DRAFT versions: re-importing replaces draft skills and items cleanly without row leakage.
   - Enforced `DATA-003` immutability: re-importing or mutating an APPROVED or REVOKED version raises `CurriculumImmutabilityError`.

4. **Approval Lifecycle & Immutable Version Forking (`app/curriculum/approval.py`):**
   - Implemented `CurriculumApprovalManager`:
     - `approve_version`: Formally stamps version with reviewer ID and timestamp; seals release as immutable (`DATA-003`).
     - `revoke_version`: Revokes previously approved releases, halting runtime session use (`FR-006`).
     - `fork_new_version`: Deep-clones approved content into a new DRAFT version for safe editorial revisions without in-place mutation (`DATA-003`).

5. **Runtime Structured Query Service (`app/curriculum/service.py`):**
   - Implemented `CurriculumService`:
     - `_verify_version_approved`: Rejects `DRAFT` versions (`CurriculumUnapprovedError`) and `REVOKED`/`ARCHIVED` versions (`CurriculumRevokedError`) per `FR-006`.
     - `get_active_activity`: Fast indexed lookup by version token and item token (`ADR-005`).
     - `get_activity_by_id`: Lookup by item UUID.
     - `get_activities_for_session`: Retrieves activities governing an active session using its pinned `curriculum_version_id`.
     - `query_activities`: Filter activities by module, skill, and difficulty.
     - Returns `ActivityProvenance` with full content, reviewer audit trail, and audio cache references.

6. **Reviewed PAUD Pilot Activity Pack (`tests/fixtures/curriculum/pilot_body_parts_pack.json`):**
   - Created reviewed Indonesian pilot pack for 4-6 year old children ("Modul 1: Mengenal Anggota Tubuh") with 4 activities: "mata", "hidung", "telinga", "mulut", complete with pedagogical review sign-off, prompts, synonyms, phonetic variations, hints, and fallback audio references.

7. **Curriculum Test Suites (`tests/unit/curriculum/`):**
   - `test_models.py`: Validates activity schema round-trip, answer token expansion, and pilot pack loading.
   - `test_importer.py`: Tests field-level error reporting, idempotent draft updates, and approved version rejection.
   - `test_approval_immutability.py`: Tests approval stamping, immutability error on approved prompt edits, version forking, and revocation.
   - `test_service.py`: Tests runtime retrieval, exact provenance DTO, draft/revoked rejection, session pinned-version retrieval, and structured filtering.

## What Remains

- Next implementation tasks in Milestone E2 (Deterministic Core):
  - `TASK-010`: Implement deterministic answer assessment (status: `READY`, dependencies: 003, 009 satisfied).
  - `TASK-006`: Enforce CI and repository quality gates (status: `READY`, dependencies: 001 satisfied).
  - `TASK-011`: Implement response planning and core safety (status: `TODO`, dependencies: 003, 009, 010).

## Files Changed

- `alembic/versions/003_curriculum_activity_spec.py` (NEW): Migration adding `activity_spec` column.
- `app/curriculum/__init__.py` (NEW): Package re-exports for models, services, importer, and exceptions.
- `app/curriculum/models.py` (NEW): Domain contracts (`CurriculumActivity`, `AnswerSpec`, `ActivityProvenance`, etc.).
- `app/curriculum/exceptions.py` (NEW): Typed domain exceptions (`CurriculumUnapprovedError`, `CurriculumImmutabilityError`, etc.).
- `app/curriculum/importer.py` (NEW): Idempotent importer with field validation and immutability guard.
- `app/curriculum/approval.py` (NEW): Approval manager, revocation, and immutable version forking.
- `app/curriculum/service.py` (NEW): Runtime structured query service with FR-006 enforcement and provenance DTO.
- `app/persistence/models.py` (MODIFIED): Added `activity_spec` column to `CurriculumItem`.
- `tests/fixtures/curriculum/pilot_body_parts_pack.json` (NEW): Reviewed Indonesian PAUD pilot pack fixture.
- `tests/unit/curriculum/__init__.py` (NEW): Test package marker.
- `tests/unit/curriculum/test_models.py` (NEW): Contract and schema roundtrip tests.
- `tests/unit/curriculum/test_importer.py` (NEW): Importer validation and idempotency tests.
- `tests/unit/curriculum/test_approval_immutability.py` (NEW): Approval, immutability, and version forking tests.
- `tests/unit/curriculum/test_service.py` (NEW): Runtime service access, draft rejection, and filtering tests.
- `tests/contract/test_device_contracts.py` (MODIFIED): Cleaned up unused type-ignore comment.
- `tasks/TASK-009.md` (MODIFIED): Marked status `DONE`, checked all 5 acceptance criteria, recorded work log.
- `tasks/index.md` (MODIFIED): Marked TASK-009 `DONE` and TASK-010 `READY`.
- `CURRENT_TASK.md` (MODIFIED): Marked status `DONE`.
- `handoffs/history/handoff-20260907-task008.md` (NEW): Archived previous handoff for TASK-008.
- `handoffs/latest.md` (MODIFIED): Updated handoff state.

## Tests Executed

- `pytest -v tests/unit/curriculum/`: **12 passed** in 0.91s.
- `pytest tests/migration/test_migrations.py`: **2 passed** in 2.03s (upgrade, downgrade to base, re-upgrade).
- `pytest`: **93 passed** in 8.63s across the complete repository test suite.
- `ruff check app tests simulators alembic`: **All checks passed** (0 errors).
- `mypy app simulators tests`: **Success: no issues found in 86 source files** (0 errors).

## Evaluation Executed

- Acceptance criteria audit for TASK-009: 5/5 criteria PASS.
- Invariants verified:
  - 100% structured PostgreSQL SQL retrieval; zero vector search or LangChain RAG (`ADR-005`, `DATA-011`).
  - Draft and revoked curriculum content strictly rejected from runtime sessions (`FR-006`).
  - Approved versions strictly immutable; pedagogical revisions require forking a new version (`DATA-003`).
  - Every retrieved activity carries complete provenance and pre-recorded offline audio references (`DATA-007`).

## Failures / Known Issues

- None.

## Decisions Made

- Added `activity_spec` JSON column to `curriculum_items` with server default `{}` via migration 003, maintaining complete backward compatibility with existing seeds while enabling rich scaffolding hints, answer specs, difficulty, and audio references.
- Structured answers into `AnswerSpec` with `exact_matches`, `synonyms`, and `phonetic_variations` to prepare for deterministic rule-based answer assessment in TASK-010 (`ADR-006`).

## Assumptions

- Downstream `TASK-010` (deterministic answer assessment) will consume the `AnswerSpec` directly from `ActivityProvenance`.
- Session orchestrator and state machine will pin sessions to approved `curriculum_version_id` and query activities via `CurriculumService`.

## Blockers

- None.

## Do Not Change

- Immutability of approved curriculum versions (`DATA-003`).
- Rejection of draft and revoked content during runtime sessions (`FR-006`).
- Structured SQL direct lookup over vector RAG (`ADR-005`, `DATA-011`).

## Exact Recommended Next Action

The next agent should claim and execute **TASK-010** (`tasks/TASK-010.md`) — "Implement deterministic answer assessment".

`TASK-010` is now in `READY` status because its dependencies (`TASK-003`, `TASK-009`) are satisfied. It implements the deterministic normalization, exact match, synonym, phonetic, and contextual rules for child answer understanding before any probabilistic fallbacks.

Alternatively eligible parallel task in `READY` status:
- `TASK-006 — Enforce CI and repository quality gates` (P0, dependencies: `TASK-001` [DONE]).

## Git State

- **Branch:** `main`
- **Latest commit:** `b3ea649 task 002 done`
- **Working tree status:** Uncommitted changes for TASK-003, TASK-004, TASK-005, TASK-007, TASK-008, and TASK-009 verified and passing all quality gates.
